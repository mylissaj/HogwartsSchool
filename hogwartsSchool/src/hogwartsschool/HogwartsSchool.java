/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package hogwartsschool;

import byui.cit260.hogwartsSchool.model.Location;

/**
 *
 * @author mylis_000
 */
public class HogwartsSchool {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Location locationOne = new Location();
        double row = 0;
        
        locationOne.setRow(row);
        double column = 0;
        locationOne.setColumn(column);
        double visited = 0;
        locationOne.setVisited(visited);
        double actor = 0;
        locationOne.setActor(actor);
        double notes = 0;
        locationOne.setNotes(notes);
        boolean locationInfo = false;
        
        System.out.println(locationInfo);
    }
    
}
